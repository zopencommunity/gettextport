diff --git a/gettext-runtime/intl/bindtextdom.c b/gettext-runtime/intl/bindtextdom.c
index c8b14bb..a22d5f2 100644
--- a/gettext-runtime/intl/bindtextdom.c
+++ b/gettext-runtime/intl/bindtextdom.c
@@ -23,6 +23,7 @@
 #include <stddef.h>
 #include <stdlib.h>
 #include <string.h>
+#include <sys/stat.h>
 
 #include "gettextP.h"
 #ifdef _LIBC
@@ -43,6 +44,10 @@
 
 #include "flexmember.h"
 
+#ifdef __MVS__
+#include <zos-base.h>
+#endif
+
 /* @@ end of prolog @@ */
 
 
@@ -402,6 +407,26 @@ BINDTEXTDOMAIN (const char *domainname, const char *dirname)
           dirname = dirname_with_drive;
         }
     }
+#endif
+#ifdef __MVS__
+  {
+    const char *textdomain_env = getenv("TEXTDOMAINDIR");
+    if (textdomain_env && textdomain_env[0] != '\0') {
+      dirname = textdomain_env;
+    } else {
+      const char *progdir = __getprogramdir();
+      if (progdir && progdir[0] != '\0') {
+        static char resolved_locale_path[PATH_MAX];
+        snprintf(resolved_locale_path, sizeof(resolved_locale_path),
+                 "%s/../share/locale", progdir);
+
+        struct stat st;
+        if (stat(resolved_locale_path, &st) == 0 && S_ISDIR(st.st_mode)) {
+          dirname = resolved_locale_path;
+        }
+      }
+    }
+  }
 #endif
   set_binding_values (domainname, &dirname, NULL, NULL);
 #ifdef __EMX__
